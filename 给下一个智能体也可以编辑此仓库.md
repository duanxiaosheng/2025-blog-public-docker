# 给下一个智能体也可以编辑此仓库

这份文档写给后续接手本仓库的 AI / 智能体。目标是让你不用重新追历史，也不要把已经踩过的坑再踩一遍。

## 1. 项目定位

本仓库是从 `YYsuni/2025-blog-public` 改造来的 Docker 自托管博客。

当前目标不是继续保留原项目的 GitHub App / `.pem` 前端编辑模式，而是：

- Docker 自托管
- 内容、图片、索引保存在本地数据目录
- 后台使用自定义管理员密码登录
- 不在前端暴露 GitHub private key / PAT / 等效敏感凭据
- 通过 Docker volume 持久化数据，容器重建后内容不丢

本地工作目录通常是：

```txt
/root/.openclaw/workspace/2025-blog-public-adapt
```

远程仓库：

```txt
GitHub: https://github.com/duanxiaosheng/2025-blog-public-docker
Gitee : https://gitee.com/duanxiaosheng/2025-blog-local-docker
```

## 2. 部署与数据目录

Docker 运行时默认数据目录：

```txt
容器内: /app/data
compose: ./data:/app/data
```

关键数据路径：

```txt
data/public/blogs/<slug>/index.md          # 文章正文
data/public/blogs/<slug>/config.json       # 文章元信息
data/public/blogs/<slug>/<image>           # 文章图片/封面
data/public/blogs/index.json               # 文章索引
data/public/blogs/categories.json          # 分类

data/public/images/<section>/<file>        # 首页/项目/分享/博客/图片集等公共图片
data/public/images/avatar.png              # 站点头像
data/public/images/favicon.png             # 站点 favicon

data/content/*.json                        # about/projects/share/bloggers/pictures/snippets/likes 等内容
data/config/site-content.json              # 站点配置
data/config/card-styles.json               # 卡片布局配置
data/config/admin-auth.json                # 首次初始化后的管理员密码 hash
```

源码 `public/` 只作为默认资源 fallback，不应作为用户运行时内容的主要存储位置。

## 3. 鉴权规则

后台管理员登录是服务端校验：

- 密码只在服务端处理
- 使用 HttpOnly session cookie
- 管理接口通过 `requireAdmin()` 拦截
- 不要恢复原项目的前端 GitHub private key / `.pem` 导入模式
- 不要把敏感配置放进 `NEXT_PUBLIC_*`

首次部署如果没有 `ADMIN_PASSWORD`，支持在后台首次初始化密码，并写入：

```txt
data/config/admin-auth.json
```

忘记密码时，删除这个文件并重启容器后可重新初始化。

## 4. 图片路径总原则

请牢记：**存储路径和展示路径不同**。

### 4.1 文章图片

存储层推荐：

```txt
/blogs/<slug>/<file>
```

展示/访问层：

```txt
/api/blogs/<slug>/<file>
```

相关归一化逻辑：

- `src/lib/load-blog.ts`
- `src/components/markdown-image.tsx`
- 写作页相关 store / hook / section

曾经出现过 `/api/api/api/blogs/...`，所以不要简单拼字符串，改路径时要注意压平重复 `/api/`。

### 4.2 公共图片

存储层：

```txt
/images/<section>/<file>
```

展示/访问层：

```txt
/api/images/<section>/<file>
```

公共内容接口读出时会把 `/images/...` 转为 `/api/images/...` 给前端展示；保存回数据时再转回 `/images/...`。

相关文件：

```txt
src/lib/local-admin/public-image-paths.ts
src/app/api/images/[...path]/route.ts
src/app/api/content/*/route.ts
src/app/api/admin/*/route.ts
```

## 5. 千万不要保存 blob/base64 临时地址

这是本项目最大坑之一。

浏览器上传预览会生成：

```txt
blob:http://...
```

它只在当前浏览器当前页面有效。保存进 JSON 后会导致：

- 当前浏览器也许还能看到
- 换浏览器 / 无痕 / 换设备必丢
- 刷新后也可能裂图

因此：

1. 图片必须先上传到服务端
2. 服务端落盘到 `data/public/...`
3. 返回 `/images/...` 存储路径
4. JSON 只能保存 `/images/...` 或外部 http(s) URL
5. 服务端要拒绝 `blob:` 写入

相关保护：

```txt
src/lib/local-admin/blob-guards.ts
src/lib/local-admin/public-image-upload-client.ts
src/app/api/admin/projects/route.ts
src/app/api/admin/share/route.ts
src/app/api/admin/bloggers/route.ts
src/app/api/admin/pictures/route.ts
```

如果用户说“当前浏览器能看，换浏览器看不到”，优先查接口返回是否还有 `blob:`：

```bash
curl -s http://<host>:3000/api/content/share
curl -s http://<host>:3000/api/content/projects
curl -s http://<host>:3000/api/content/bloggers
curl -s http://<host>:3000/api/content/pictures
```

## 6. favicon / avatar 是特殊的可变站点资源

favicon 和 avatar 不能按普通静态资源长期缓存处理。用户要求实时更新，不要旧浏览器刷新后回旧图。

最终实现为专用可变资源链路：

```txt
/api/site-assets/avatar?v=<version>
/api/site-assets/favicon?v=<version>
/api/site-assets/version
```

版本号来自文件 `mtime + size`，上传后会变化。响应头必须禁缓存：

```txt
Cache-Control: no-store, no-cache, must-revalidate, max-age=0
Pragma: no-cache
Expires: 0
```

相关文件：

```txt
src/lib/local-admin/site-assets.ts
src/app/api/site-assets/avatar/route.ts
src/app/api/site-assets/favicon/route.ts
src/app/api/site-assets/version/route.ts
src/app/favicon.png/route.ts
src/hooks/use-site-assets.ts
src/layout/head.tsx
src/layout/index.tsx
src/app/(home)/hi-card.tsx
src/components/nav-card.tsx
src/app/(home)/config-dialog/site-settings/favicon-avatar-upload.tsx
src/app/(home)/services/push-site-content.ts
```

兼容旧路径：

- `/favicon.png` 仍然存在，但动态读取 `data/public/images/favicon.png`
- `/api/images/avatar.png` 和 `/api/images/favicon.png` 也会走同一动态读取逻辑

如果 favicon/avatar 出问题，优先检查：

```bash
curl -I http://<host>:3000/api/site-assets/avatar
curl -I http://<host>:3000/api/site-assets/favicon
curl -s http://<host>:3000/api/site-assets/version
curl -I http://<host>:3000/favicon.png
```

正常应看到 `no-store`，且上传后 version 变化。

## 7. 首页/项目/分享/博客/图片集上传链路

常见内容页面：

```txt
/projects   -> data/content/projects.json   -> /api/content/projects
/share      -> data/content/share.json      -> /api/content/share
/bloggers   -> data/content/bloggers.json   -> /api/content/bloggers
/pictures   -> data/content/pictures.json   -> /api/content/pictures
```

这些页面的新增/编辑图片，需要走：

```txt
/api/admin/upload-public-image
```

然后最终保存 JSON。

保存后前端状态必须回写服务端返回的真实路径，不要继续保留 preview blob。

## 8. 文章图片上传链路

不要把图片 base64 和整篇文章一起塞进单个 JSON 保存请求。

更稳做法：

1. 图片单独上传
2. 服务端 Buffer 落盘
3. 返回 `/blogs/<slug>/<file>`
4. markdown 替换占位符
5. 最后保存文章正文和元信息

文章图片接口：

```txt
src/app/api/admin/upload-image/route.ts
src/app/api/admin/blogs/route.ts
src/app/api/blogs/[...path]/route.ts
src/app/api/blogs/[slug]/[asset]/route.ts
```

## 9. API / 存储相关核心文件

```txt
src/lib/local-admin/storage.ts              # DATA_DIR、seed、读写 json、保存图片
src/lib/local-admin/http.ts                 # 管理员 session / requireAdmin
src/lib/local-admin/auth.ts                 # 密码 hash / 校验
src/lib/local-admin/public-image-paths.ts   # /images 与 /api/images 双向归一化
src/lib/local-admin/blob-guards.ts          # 拒绝 blob 写入
src/lib/local-admin/site-assets.ts          # favicon/avatar 可变资源
```

## 10. Docker / 构建

当前 Dockerfile 使用 Next standalone：

```txt
next.config.mjs -> output: 'standalone'
容器启动 -> node server.js
```

镜像瘦身后 runner 只复制：

```txt
.next/standalone
.next/static
public
```

不要把运行时数据写进镜像内不可持久目录，应写入 `DATA_DIR`。

## 11. 验证流程

修改代码后至少跑：

```bash
npm run build
```

数据与图片链路改动后必须跑：

```bash
npm run check:data
```

`check:data` 会检查：

- JSON / markdown 中是否混入 `blob:` 或 `data:image`
- 存储层是否错误保存了 `/api/images/...`、`/api/blogs/...`
- 本地图片引用是否真实存在
- 文章 `index.json` 是否引用不存在的文章目录
- markdown 中是否出现 `/api/api/`
- favicon/avatar fallback 资源是否存在

图片问题也可以附加验证：

```bash
curl -s http://localhost:3000/api/content/share | grep blob || true
curl -I http://localhost:3000/api/site-assets/avatar
curl -I http://localhost:3000/api/site-assets/favicon
curl -s http://localhost:3000/api/site-assets/version
```

如果有线上地址，也可以直接查线上：

```bash
curl -s http://81.70.46.21:3000/api/content/share
curl -I http://81.70.46.21:3000/api/site-assets/avatar
curl -I http://81.70.46.21:3000/api/site-assets/favicon
```

## 12. Git 工作流

修复后默认：

```bash
npm run build
git status --short
git add <changed-files>
git commit -m "..."
git push origin main
git push gitee main
```

如果 Gitee 提示 fetch first，不要硬推，先：

```bash
git fetch gitee main
git status
git log --oneline --graph --decorate --all -20
```

确认差异后再 merge/rebase。

## 13. 与用户沟通偏好

用户叫小东。沟通要直接、自然、像朋友，不要客服腔。

遇到这个项目的问题，不要让用户重复背景，优先：

1. 进入项目目录
2. 查接口 / 代码 / 线上响应
3. 修复
4. `npm run build`
5. commit + push GitHub/Gitee
6. 简短说明根因和部署注意点

不要在未验证前说“好了”。

## 14. 最近确认可用的重要提交

```txt
8efb246 Rewrite mutable site asset handling
```

这个提交后，用户确认 favicon/avatar 实时更新问题“可以了”。

此前相关提交：

```txt
5021d9f Fix favicon and avatar runtime uploads
cb030a8 Serve favicon from runtime data
12a7acf Harden public image upload persistence
cc6d70a Fix public image uploads from create dialogs
ed08828 Fix local public image URL normalization
```
