# 给下一个智能体也可以编辑此仓库

## 接手前 2 分钟必读摘要

如果你是一个**全新**接手这个仓库的 OpenClaw / AI，请先记住下面 10 条，再开始动手：

1. **这不是原始 GitHub App 编辑器项目了。**
   当前方向是：Docker 自托管 + 本地文件存储 + 服务端管理员密码登录。

2. **不要恢复 `.pem` / GitHub App / 前端签 JWT / 前端直写 GitHub 仓库`。**
   这是旧路线，不是当前项目目标。

3. **运行时数据不在源码里，在 `data/` 里。**
   真正要读写的是：
   - `data/config/*`
   - `data/content/*`
   - `data/public/*`

4. **图片路径分两层：存储路径 ≠ 展示路径。**
   - 公共图片：存储 `/images/...`，展示 `/api/images/...`
   - 文章图片：存储 `/blogs/...`，展示 `/api/blogs/...`

5. **绝对不要把 `blob:` / base64 临时图写进 JSON。**
   图片必须先上传到服务端，落盘后再保存真实路径。

6. **管理员密码只在服务端校验。**
   会写入 `data/config/admin-auth.json`，管理接口通过 session cookie 鉴权。

7. **favicon / avatar 是特殊动态资源，不是普通静态图片。**
   它们走 `/api/site-assets/*`，并且必须禁缓存。

8. **如果用户说图片丢了、换浏览器看不到，优先怀疑路径或 blob。**
   这在本项目里是高频坑。

9. **如果用户说首页或顶部导航手感怪，优先看 `src/components/nav-card.tsx`。**
   这个文件最近连续修过：首页不默认 active、hover 白色高亮、图标替换、导航顺序、间距等。

10. **每次改完都至少跑构建验证。**
    本项目使用 pnpm，优先跑 `pnpm build`；如果涉及图片 / 数据链路，再跑 `pnpm check:data`。

如果你只剩 30 秒，那就先读这几个文件：

```txt
src/components/nav-card.tsx
src/lib/local-admin/storage.ts
src/lib/local-admin/content-repository.ts
src/lib/local-admin/public-image-paths.ts
src/lib/local-admin/site-assets.ts
给下一个智能体也可以编辑此仓库.md
```

---

## 近期重要更新（2026-05-18）

这一版已经完成一轮较大的导航、写作页和 Docker 体验优化。后续智能体接手时请注意：

### 导航与页面加载

- `src/components/nav-card.tsx` 里，首页 `/` 不再默认选中“我的文章”，所以首页默认不显示白色 active 滑块。
- 首页 hover 导航项时仍然要显示白色滑块，不要误删 hover 逻辑。
- 顶部 icons 导航间距已经调紧：桌面 `12`，移动 `8`，容器 padding `8`。
- 导航顺序现在是：我的文章、应用导航、我的项目、优秀博客、推荐分享、关于网站。
- 多个内容页已经避免首屏先渲染源码里的旧 `list.json`，再闪成 API 数据。相关页面包括 apps/projects/share/bloggers/pictures/snippets。

### 导航 SVG 图标

导航图标已经统一替换过，文件在：

```txt
src/svgs/scroll-*.svg
src/svgs/apps-*.svg
src/svgs/projects-*.svg
src/svgs/about-*.svg
src/svgs/share-*.svg
src/svgs/website-*.svg
```

如果用户继续提供 zip 图标包，通常需要把中文目录名映射到这些实际文件名。不要只按 zip 内通用文件名覆盖，否则容易错位。

### 写文章 / 编辑文章图片体验

相关文件：

```txt
src/app/write/stores/write-store.ts
src/app/write/components/editor.tsx
src/app/write/components/sections/images-section.tsx
```

当前要求：

- 保留桌面拖拽图片到编辑器的能力。
- 同时保留移动端友好的点击按钮插入图片能力。
- 图片按钮文案是 `填 入`，两个字中间有空格。
- `填 入` 是红色、加粗、比最初版本稍大一点。
- 按钮必须在缩略图下方，不要放回图片内部。
- 封面覆盖层是整张图的白色磨砂遮罩，当前用 inline style 控制透明度和 blur。
- “封面”两个字是红色、加粗。

### 单篇文章编辑页右上角功能栏

用户说“某一篇文章右上角编辑模式”，通常指的是：

```txt
/blog/[id] 文章详情页点击右上角“编辑”
进入 /write/[slug]
然后看 /write/[slug] 右上角功能栏
```

不是 `/blog` 文章列表页的批量编辑模式。不要再混淆。

相关文件：

```txt
src/app/write/components/actions.tsx
```

当前实现：

- 功能栏固定在右上角：`fixed top-4 right-6 z-[9999]`。
- 删除 / 取消按钮已经改成原生 `<button>`，避免 motion 或浏览器 confirm 导致点击不稳定。
- 点击“取消”直接回到原文章页，不再弹浏览器 confirm。
- 点击“删除”后显示页面内确认条，不再用浏览器 confirm。
- 删除确认条要浮在“删除”按钮正下方：`absolute top-full right-0 mt-2`。
- 确认条不能挤压功能栏布局。

### Docker 构建

`Dockerfile` 已经改为 pnpm 安装依赖：

```txt
corepack enable
corepack prepare pnpm@10.15.0 --activate
pnpm install --frozen-lockfile
```

不要轻易改回 npm。之前 npm 在容器里出现过 `Exit handler never called!`。

### 当前远程与部署

本地真实项目路径是：

```txt
/root/2025-blog-local-docker
```

远程：

```txt
github -> git@github.com:duanxiaosheng/2025-blog-public-docker.git
origin -> git@gitee.com:duanxiaosheng/2025-blog-local-docker.git
```

本地 Docker 容器：

```txt
container: 2025-blog-local-docker
service: blog
port: 3000:3000
```

UI 改动如果需要让本地站点立刻可见，提交推送后还要执行：

```bash
DOCKER_BUILDKIT=0 docker compose up -d --build
```

---

这份文档写给后续接手本仓库的 AI / 智能体，也写给未来会回来看这个项目的人。

目标只有一个：**让接手者不用重新猜项目本质、不用重新踩老坑、能直接继续维护。**

---

# 0. 一句话结论

这个项目已经不是原始的 `2025-blog-public` 了。

它现在的本质是：

> **一个基于 Next.js 的 Docker 自托管个人博客 / 可视化后台，内容、图片、索引都落本地数据目录，后台采用服务端管理员密码登录。**

不要再把它当成“前端直连 GitHub 的博客编辑器”。
不要再往 GitHub App、`.pem`、前端签 JWT、前端直写仓库那条路回退。

---

# 1. 项目定位与改造方向

## 1.1 原项目本质

原始项目 `YYsuni/2025-blog-public` 更像：

- 前端可视化博客编辑器
- 浏览器端导入 GitHub App private key / `.pem`
- 前端签 JWT
- 再调 GitHub API 提交内容

它不是传统“服务端后台 + 本地存储”的博客系统。

## 1.2 本仓库现在的目标

本仓库已经明确转向：

- Docker 自托管
- 内容保存在本地文件系统
- 图片保存在本地文件系统
- 管理员密码在服务端校验
- HttpOnly session cookie
- Docker volume 持久化数据
- 不把敏感凭据暴露给前端

## 1.3 不要走回头路

以下方向都不推荐：

- 把 `.pem` / PAT 换个名字伪装成“密码”
- 恢复前端直接写 GitHub 仓库
- 用 `NEXT_PUBLIC_*` 暴露服务端敏感配置
- 把运行时内容重新写回源码 `public/` 或 `src/app/*/list.json`

---

# 2. 仓库位置与远程仓库

本地项目目录：

```txt
/root/2025-blog-local-docker
```

远程仓库：

```txt
GitHub: https://github.com/duanxiaosheng/2025-blog-public-docker
Gitee : https://gitee.com/duanxiaosheng/2025-blog-local-docker
```

当前分支通常是：

```txt
main
```

---

# 3. 运行方式与部署方式

## 3.1 本地开发

```bash
cd /root/2025-blog-local-docker
pnpm dev
```

默认开发端口：

```txt
http://localhost:2025
```

## 3.2 构建

```bash
pnpm build
```

## 3.3 生产运行

```bash
pnpm start
```

## 3.4 Docker

`Dockerfile` 使用 Next standalone 方案。

关键点：

- builder 阶段执行 `pnpm build`
- runner 阶段只复制：
  - `public`
  - `.next/static`
  - `.next/standalone`
- 最终通过 `node server.js` 启动

相关文件：

```txt
Dockerfile
docker-compose.yml
next.config.mjs
```

## 3.5 docker-compose

当前 compose 设计：

```yaml
services:
  blog:
    build: .
    container_name: 2025-blog-local-docker
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3000
      DATA_DIR: ${DATA_DIR:-/app/data}
    volumes:
      - ./data:${DATA_DIR:-/app/data}
    restart: unless-stopped
```

说明：

- 默认**不预置** `ADMIN_PASSWORD`
- 默认**不预置** `SESSION_SECRET`
- 首次访问后台/编辑页时初始化管理员密码
- 若未传 `SESSION_SECRET`，程序会自动生成并持久化到 `data/config/admin-auth.json`

结论：

- 容器内数据目录默认 `/app/data`
- 宿主机默认挂载 `./data`
- **运行时数据必须写入 `DATA_DIR`，不要写进镜像层**

---

# 4. 数据目录与持久化结构

## 4.1 运行时数据根目录

```txt
data/
```

## 4.2 目录职责总览

```txt
data/
├── config/
│   ├── admin-auth.json        # 管理员密码 hash / 初始化结果
│   ├── card-styles.json       # 首页各卡片布局、尺寸、启用状态
│   └── site-content.json      # 站点标题、头像、favicon、背景图、开关等
├── content/
│   ├── about.json             # 关于页内容
│   ├── apps.json              # 应用导航数据
│   ├── bloggers.json          # 优秀博客数据
│   ├── likes.json             # 点赞持久化数据
│   ├── pictures.json          # 图片集数据
│   ├── projects.json          # 项目数据
│   ├── share.json             # 推荐分享数据
│   └── snippets.json          # 代码片段数据
└── public/
    ├── blogs/
    │   ├── index.json         # 文章索引
    │   ├── categories.json    # 分类
    │   └── <slug>/
    │       ├── index.md       # 文章正文
    │       ├── config.json    # 标题/摘要/标签/封面等
    │       └── <assets>       # 当前文章的图片资源
    └── images/
        └── <section>/         # projects/share/bloggers/apps/pictures 等公共图片
```

## 4.3 源码目录与运行时目录的关系

源码中的这些文件只是默认 seed 或 fallback：

```txt
public/
src/app/*/list.json
src/config/site-content.json
src/config/card-styles.json
```

真正运行后用户修改的数据，应该在：

```txt
data/config/*
data/content/*
data/public/*
```

**不要把运行时修改重新写回源码默认文件。**

---

# 5. 项目文件结构：按职责理解

下面不是简单列文件，而是按“干什么”来分。

---

## 5.1 页面层 `src/app/`

### 首页与首页卡片

```txt
src/app/(home)/page.tsx                    # 首页装配入口
src/app/(home)/hi-card.tsx                # 主欢迎卡
src/app/(home)/art-card.tsx               # 艺术图/大图卡
src/app/(home)/aritcle-card.tsx           # 最新文章卡
src/app/(home)/share-card.tsx             # 推荐分享卡
src/app/(home)/clock-card.tsx             # 时钟卡
src/app/(home)/calendar-card.tsx          # 日历卡
src/app/(home)/like-position.tsx          # 点赞位置卡
src/app/(home)/write-buttons.tsx          # 写作快捷入口
src/app/(home)/social-buttons.tsx         # 社交按钮
src/app/(home)/hat-card.tsx               # 圣诞帽等装饰卡
src/app/(home)/beian-card.tsx             # 备案信息卡
src/app/(home)/home-draggable-layer.tsx   # 首页拖拽布局层
```

### 首页配置弹窗 / 布局编辑

```txt
src/app/(home)/config-dialog/index.tsx
src/app/(home)/config-dialog/home-layout.tsx
src/app/(home)/stores/config-store.ts
src/app/(home)/stores/layout-edit-store.ts
```

作用：

- 编辑首页卡片布局与显示内容
- 站点设置、图片设置、资源清理入口
- 维护 `site-content` 与 `card-styles`

### 内容页

```txt
src/app/about/page.tsx                    # 关于页
src/app/about/page-client.tsx             # 关于页客户端渲染
src/app/projects/page.tsx                 # 项目页
src/app/share/page.tsx                    # 推荐分享页
src/app/bloggers/page.tsx                 # 优秀博客页
src/app/pictures/page.tsx                 # 图片集页
src/app/snippets/page.tsx                 # 代码片段页
src/app/apps/page.tsx                     # 应用导航页
src/app/blog/page.tsx                     # 文章列表页
src/app/blog/[id]/page.tsx                # 文章详情页
src/app/write/page.tsx                    # 新建文章页
src/app/write/[slug]/page.tsx             # 编辑文章页
```

### 页面级组件目录

```txt
src/app/apps/components/*
src/app/bloggers/components/*
src/app/projects/components/*
src/app/share/components/*
src/app/write/components/*
src/app/blog/components/*
```

作用：

- 每个内容页自己的卡片、编辑弹窗、上传弹窗、辅助 UI

---

## 5.2 API 层 `src/app/api/`

### 管理员接口 `/api/admin/*`

这些是写操作 / 登录态相关接口。

```txt
src/app/api/admin/init/route.ts           # 首次初始化管理员密码
src/app/api/admin/login/route.ts          # 管理员登录
src/app/api/admin/logout/route.ts         # 管理员退出
src/app/api/admin/session/route.ts        # 当前登录态
src/app/api/admin/config/route.ts         # 保存站点配置/布局配置
src/app/api/admin/blog-index/route.ts     # 文章索引保存
src/app/api/admin/blogs/route.ts          # 创建/保存文章
src/app/api/admin/blogs/[slug]/route.ts   # 删除单篇文章等
src/app/api/admin/upload-image/route.ts   # 文章图片上传
src/app/api/admin/upload-public-image/route.ts # 公共图片上传
src/app/api/admin/about/route.ts
src/app/api/admin/apps/route.ts
src/app/api/admin/bloggers/route.ts
src/app/api/admin/pictures/route.ts
src/app/api/admin/projects/route.ts
src/app/api/admin/share/route.ts
src/app/api/admin/snippets/route.ts
src/app/api/admin/assets/route.ts         # 资源管理/清理器
```

### 公开读取接口 `/api/content/*`

```txt
src/app/api/content/about/route.ts
src/app/api/content/apps/route.ts
src/app/api/content/bloggers/route.ts
src/app/api/content/pictures/route.ts
src/app/api/content/projects/route.ts
src/app/api/content/share/route.ts
src/app/api/content/snippets/route.ts
src/app/api/content/config/route.ts
```

作用：

- 把 `data/content/*.json` 读给前端
- 同时把存储路径规范化成前端展示路径

### 文章读取与资源读取接口

```txt
src/app/api/blogs/[...path]/route.ts
src/app/api/blogs/[slug]/[asset]/route.ts
src/app/api/blogs/[slug]/config.json/route.ts
src/app/api/blogs/[slug]/index.md/route.ts
src/app/api/images/[...path]/route.ts
```

作用：

- 读取文章 markdown/config/assets
- 读取公共图片
- 支持数据目录优先、源码 fallback

### 站点配置读取接口

```txt
src/app/api/config/site-content/route.ts
src/app/api/config/card-styles/route.ts
```

### 站点资源接口

```txt
src/app/api/site-assets/avatar/route.ts
src/app/api/site-assets/favicon/route.ts
src/app/api/site-assets/version/route.ts
src/app/favicon.png/route.ts
```

作用：

- 专门服务动态 avatar / favicon
- 解决缓存问题

### 其他接口

```txt
src/app/api/likes/route.ts                 # 点赞持久化
```

---

## 5.3 复用组件层 `src/components/`

```txt
src/components/nav-card.tsx                # 首页左上导航卡（非常重要）
src/components/card.tsx                    # 卡片基础容器
src/components/blog-preview.tsx            # 文章详情内容渲染外壳
src/components/blog-sidebar.tsx            # 文章详情侧边栏
src/components/blog-toc.tsx                # 目录
src/components/markdown-image.tsx          # markdown 图片渲染与路径归一化
src/components/like-button.tsx             # 点赞按钮
src/components/admin-password-dialog.tsx   # 管理员登录弹窗
src/components/star-rating.tsx
src/components/editable-star-rating.tsx
src/components/color-picker*.tsx
src/components/dialog-modal.tsx
src/components/select.tsx
```

### `nav-card.tsx` 现在特别重要

这个文件近期连续改了几次，原因是它问题最多、视觉联动最多。

它负责：

- 首页左上导航卡
- icons 模式 / full 模式切换
- apps 图标单独放大
- hover 白色高亮位置与尺寸
- 导航项点击热区
- 新增 `/apps` 入口

如果以后导航栏再出问题，**优先看这个文件，不要先乱怀疑路由。**

---

## 5.4 hooks 层 `src/hooks/`

```txt
src/hooks/use-auth.ts                      # 管理员登录态
src/hooks/use-blog-index.ts                # 文章索引读取
src/hooks/use-categories.ts                # 分类读取
src/hooks/use-center.ts                    # 首页卡片中心点计算
src/hooks/use-markdown-render.tsx          # markdown 渲染 hook
src/hooks/use-read-articles.ts             # 文章阅读统计相关
src/hooks/use-site-assets.ts               # avatar/favicon 动态版本 URL
src/hooks/use-size.ts                      # 响应式尺寸判断（maxSM/maxXS 等）
```

其中：

- `use-size.ts` 影响移动端、icons 顶栏、布局切换
- `use-site-assets.ts` 影响头像 / favicon 实时更新链路

---

## 5.5 核心逻辑层 `src/lib/`

### 内容与存储：最关键

```txt
src/lib/local-admin/storage.ts
```

这是核心中的核心，负责：

- 解析 `DATA_DIR`
- 初始化数据目录
- 首次 seed 默认内容
- 读写 JSON
- 原子写文件
- 博客索引读写
- 公共资源删除
- 文章目录与资源读写

如果运行时数据目录、seed、文件落盘出问题，先看它。

### 内容集合仓库

```txt
src/lib/local-admin/content-repository.ts
```

负责：

- 声明有哪些内容集合：`about / projects / share / bloggers / pictures / snippets / apps`
- 定义每个集合对应哪个 JSON 文件
- 哪些字段是图片字段
- 读取时做展示路径归一化
- 保存时做存储路径归一化
- 拒绝 blob

### 登录与权限

```txt
src/lib/local-admin/auth.ts
src/lib/local-admin/http.ts
```

负责：

- 密码 hash / 校验
- session cookie
- `requireAdmin()`

### 图片路径与 blob 保护

```txt
src/lib/local-admin/public-image-paths.ts
src/lib/local-admin/public-image-upload-client.ts
src/lib/local-admin/blob-guards.ts
```

负责：

- `/images/...` 与 `/api/images/...` 的双向转换
- 上传后把前端 preview 路径替换为真实路径
- 阻止 `blob:` 被写入 JSON

### 可变站点资源

```txt
src/lib/local-admin/site-assets.ts
```

负责：

- avatar/favicon 版本计算
- 动态资源读取与缓存控制

### 资源清理器

```txt
src/lib/local-admin/asset-inventory.ts
src/lib/local-admin/assets.ts
```

负责：

- 扫描 `data/public` 下资源
- 收集引用关系
- 标记已使用 / 未使用 / 受保护 / 可删除

### 文章读取

```txt
src/lib/load-blog.ts
src/lib/blog-index.ts
src/lib/markdown-renderer.ts
```

负责：

- 文章索引与详情读取
- markdown 渲染
- 封面 / 正文资源路径归一化

---

# 6. 当前功能地图：页面与数据怎么对应

| 页面 | 数据源 | 管理接口 | 资源字段 |
|---|---|---|---|
| `/about` | `data/content/about.json` | `/api/admin/about` | 无 |
| `/projects` | `data/content/projects.json` | `/api/admin/projects` | `image` |
| `/share` | `data/content/share.json` | `/api/admin/share` | `logo` |
| `/bloggers` | `data/content/bloggers.json` | `/api/admin/bloggers` | `avatar` |
| `/pictures` | `data/content/pictures.json` | `/api/admin/pictures` | `image/images` |
| `/snippets` | `data/content/snippets.json` | `/api/admin/snippets` | 无 |
| `/apps` | `data/content/apps.json` | `/api/admin/apps` | `icon` |
| `/blog` | `data/public/blogs/index.json` | `/api/admin/blogs` / `/api/admin/blog-index` | `cover` / markdown images |

---

# 7. 图片路径规则：这是最大坑，必须记住

## 7.1 文章资源

### 存储层

```txt
/blogs/<slug>/<file>
```

### 展示层

```txt
/api/blogs/<slug>/<file>
```

### 典型出错方式

- 保存成 `/api/blogs/...`
- 拼出 `/api/api/blogs/...`
- markdown 中直接混入最终展示 URL

### 相关文件

```txt
src/lib/load-blog.ts
src/components/markdown-image.tsx
src/app/blog/[id]/page.tsx
src/app/write/components/*
```

---

## 7.2 公共图片资源

### 存储层

```txt
/images/<section>/<file>
```

### 展示层

```txt
/api/images/<section>/<file>
```

### 相关文件

```txt
src/lib/local-admin/public-image-paths.ts
src/app/api/images/[...path]/route.ts
src/lib/local-admin/content-repository.ts
```

---

## 7.3 绝对不要保存这些东西

```txt
blob:http://...
data:image/png;base64,...
/api/images/...
/api/blogs/...
```

可以保存的：

```txt
/images/...
/blogs/...
https://example.com/...
http://example.com/...
```

---

# 8. 管理员登录与初始化规则

## 8.1 首次初始化

如果未设置管理员密码，支持首次初始化。

密码 hash 会写到：

```txt
data/config/admin-auth.json
```

## 8.2 登录态

- 服务端校验
- HttpOnly session cookie
- 管理接口通过 `requireAdmin()` 拦截

## 8.3 忘记密码处理

删除：

```txt
data/config/admin-auth.json
```

然后重启容器，再重新初始化。

---

# 9. 当前已经踩过并确认的坑

下面这些不是“可能”，而是已经踩过。

---

## 9.1 图片上传后当前浏览器能看，换浏览器失效

### 根因

保存进 JSON 的是 `blob:` 临时地址。

### 正确做法

1. 先上传到服务端
2. 服务端落盘
3. 返回 `/images/...` 或 `/blogs/...`
4. 前端状态替换成真实路径
5. 再保存 JSON

### 相关文件

```txt
src/lib/local-admin/blob-guards.ts
src/lib/local-admin/public-image-upload-client.ts
src/lib/local-admin/content-repository.ts
```

---

## 9.2 favicon/avatar 更新后浏览器还是旧图

### 根因

把它们当普通静态资源缓存了。

### 解决方案

专门使用：

```txt
/api/site-assets/avatar?v=<version>
/api/site-assets/favicon?v=<version>
/api/site-assets/version
```

并强制 `no-store`。

---

## 9.3 文章图片 / 封面 404

### 根因候选

- 存储路径用错成 `/api/blogs/...`
- slug 丢失
- `/api/api/...` 重复前缀
- 文件根本没落盘

### 优先检查

```txt
data/public/blogs/<slug>/index.md
data/public/blogs/<slug>/config.json
data/public/blogs/<slug>/<asset>
```

---

## 9.4 首页图片或背景图 404

### 根因

公共图片展示路径和存储路径混用。

### 解决方案

统一：

- 存储 `/images/...`
- 展示 `/api/images/...`

并在 `/api/images/[...path]` 支持源码 fallback。

---

## 9.5 封面二次编辑失效

### 根因

封面上传链路错误复用正文图片逻辑，去重/替换行为不对。

### 处理结论

封面上传应有独立处理，不要直接混到正文图片去重逻辑里。

---

## 9.6 资源清理误删

### 根因候选

- 没扫描到新 JSON 字段
- 路径没有从 `/api/...` 正确归一化回存储路径
- 受保护资源名单不全

### 特别注意

```txt
avatar.png
favicon.png
```

是受保护资源，不能删。

---

## 9.7 移动端文章详情宽度不一致

### 解决记录

`src/components/blog-preview.tsx` 去掉 `max-sm:px-0`，恢复移动端外层边距一致性。

---

## 9.8 搜索框太丑

### 已统一过的页面

```txt
src/app/bloggers/grid-view.tsx
src/app/share/grid-view.tsx
src/app/apps/grid-view.tsx
```

风格关键词：

- 左图标
- 右清空按钮
- `rounded-2xl`
- 玻璃卡片感
- 品牌色 focus ring

后续新加搜索框建议复用。

---

## 9.9 首页导航栏近期连续踩坑记录（2026-05-14）

这是最近最值得记录的一组问题。

### 现象 1：导航点击经常点不到 / 需要点很多次

#### 根因

- icons 模式下实际点击热区太小
- apps 图标视觉放大后，图标看起来变大，但链接命中区域没同步变大
- hover 白色高亮层存在拦截点击风险

#### 处理

- 给 icons 模式导航项固定更可靠的点击热区（`h-11 w-11`）
- hover 高亮层加 `pointer-events-none`

### 现象 2：优秀博客卡片看起来能点，但点击不稳定

#### 根因

卡片内交互结构不够合理，头像 / 标题的链接行为不够明确。

#### 处理

- 头像在非编辑态变为明确链接
- 标题在非编辑态变为明确链接
- 编辑态继续保留可编辑结构

相关文件：

```txt
src/app/bloggers/components/blogger-card.tsx
```

### 现象 3：导航栏 apps 图标需要单独放大

#### 处理

- 只在 `src/components/nav-card.tsx` 中对 `/apps` 项单独应用 `APPS_ICON_SCALE = 1.5`
- 不要误改 `/apps` 页面应用卡片里的图标尺寸

### 现象 4：hover 白色高亮不居中

#### 根因

最早按旧的 `itemHeight=28` 算高亮；后来虽然改成读元素位置，但仍没有完全按真实按钮尺寸计算。

#### 正确结论

hover 高亮必须按真实 DOM 的：

- `offsetLeft`
- `offsetTop`
- `offsetWidth`
- `offsetHeight`

来算，而不是拿旧尺寸估算。

### 现象 5：hover 白色高亮太大

#### 处理

`extraSize` 从 `8` 调小为 `5`。

### 现象 6：导航图标间距太大

#### 处理

icons 模式下：

- 外层 gap 收紧
- 内部 gap 收紧

#### 当前相关提交

```txt
e430ab0 Fix nav click hit area and blogger link targets
e64bb04 Fix nav hover highlight alignment
9ea52c7 Tweak nav hover highlight size
a748cd6 Tighten nav icon spacing
```

如果以后导航栏再出问题，**优先从这些提交往回看。**

---

# 10. 应用导航 `/apps` 功能说明

这是新增功能，已经不是原项目里天然有的。

## 10.1 用户要的东西

用户要的不是站点设置里的一个静态配置项，而是：

> 像“我的项目 / 推荐分享”一样，有独立页面、有后台编辑能力、有图标宫格展示的“应用导航”页面。

## 10.2 当前实现

- 首页导航新增「应用导航」入口
- 页面地址：`/apps`
- 数据文件：`data/content/apps.json`
- 图标字段：`icon`
- 图标上传 section：`apps`
- 存储路径：`/images/apps/<file>`
- 展示路径：`/api/images/apps/<file>`

## 10.3 相关文件

```txt
src/app/apps/page.tsx
src/app/apps/grid-view.tsx
src/app/apps/components/app-card.tsx
src/app/apps/components/create-dialog.tsx
src/app/apps/components/icon-upload-dialog.tsx
src/app/apps/services/push-apps.ts
src/app/apps/list.json
src/app/api/content/apps/route.ts
src/app/api/admin/apps/route.ts
src/lib/local-admin/content-repository.ts
src/lib/local-admin/storage.ts
src/lib/local-admin/assets.ts
src/components/nav-card.tsx
```

## 10.4 当前字段

```ts
{
  name: string
  icon: string
  url: string
  visible?: boolean
  description?: string
  tags?: string[]
}
```

## 10.5 当前 UI 约束

- 显示形式是 App 图标宫格，不是推荐分享卡片
- 不用评星
- 可以有描述，用于 hover 提示
- 可以有 tags，用于筛选
- 当前支持搜索与标签分类

---

# 11. 资源管理 / 资源清理器

后台已有“资源管理 / 资源清理”入口。

## 11.1 相关文件

```txt
src/app/(home)/config-dialog/site-settings/asset-cleaner-section.tsx
src/app/(home)/config-dialog/site-settings/asset-cleaner-dialog.tsx
src/app/api/admin/assets/route.ts
src/lib/local-admin/asset-inventory.ts
```

## 11.2 它会做什么

- 扫描 `data/public` 下的运行时图片
- 收集 `data/content/*.json`、`data/config/*.json`、文章 markdown/config 里的引用
- 把 `/api/images/...`、`/api/blogs/...` 归一化回存储路径
- 标记是否已使用 / 是否受保护 / 是否可删除

## 11.3 删除规则

- 已使用资源不可删
- 受保护资源不可删
- 只有明确未使用资源才可删
- 删除后要同步清理 `site-content.json` 中相关引用

---

# 12. 当前重要配置 / 默认值来源

## 12.1 站点配置

```txt
src/config/site-content.json      # 源码默认
src/config/card-styles.json       # 源码默认

data/config/site-content.json     # 运行时真实配置
data/config/card-styles.json      # 运行时真实配置
```

## 12.2 默认 seed 内容来源

由 `src/lib/local-admin/storage.ts` 负责初始化：

- `DEFAULT_ABOUT`
- `DEFAULT_PROJECTS`
- `DEFAULT_SHARE`
- `DEFAULT_APPS`
- `DEFAULT_BLOGGERS`
- `DEFAULT_PICTURES`
- `likes.json`
- `blogs/index.json`
- `blogs/categories.json`

如果后续要改“新部署默认展示内容”，应优先改这里。

---

# 13. 哪些文件最值得优先读

如果只能快速读一批文件，建议按这个顺序：

## 第一优先级：先理解项目骨架

```txt
package.json
Dockerfile
docker-compose.yml
src/lib/local-admin/storage.ts
src/lib/local-admin/content-repository.ts
src/lib/local-admin/http.ts
src/lib/local-admin/auth.ts
```

## 第二优先级：理解图片与资源路径

```txt
src/lib/local-admin/public-image-paths.ts
src/lib/local-admin/blob-guards.ts
src/lib/local-admin/public-image-upload-client.ts
src/lib/load-blog.ts
src/components/markdown-image.tsx
src/app/api/images/[...path]/route.ts
src/app/api/blogs/[...path]/route.ts
```

## 第三优先级：理解首页与导航

```txt
src/app/(home)/page.tsx
src/components/nav-card.tsx
src/app/(home)/home-draggable-layer.tsx
src/app/(home)/stores/config-store.ts
src/app/(home)/stores/layout-edit-store.ts
```

## 第四优先级：理解页面级 CRUD

```txt
src/app/projects/page.tsx
src/app/share/page.tsx
src/app/bloggers/page.tsx
src/app/apps/page.tsx
src/app/write/page.tsx
src/app/write/[slug]/page.tsx
```

---

# 14. 修改某类问题时，应该先看哪里

## 14.1 登录 / 后台权限问题

```txt
src/lib/local-admin/auth.ts
src/lib/local-admin/http.ts
src/app/api/admin/login/route.ts
src/app/api/admin/session/route.ts
src/components/admin-password-dialog.tsx
```

## 14.2 公共图片保存错 / blob 问题

```txt
src/lib/local-admin/content-repository.ts
src/lib/local-admin/public-image-paths.ts
src/lib/local-admin/blob-guards.ts
src/lib/local-admin/public-image-upload-client.ts
src/app/api/admin/upload-public-image/route.ts
```

## 14.3 文章图 / 封面问题

```txt
src/app/api/admin/upload-image/route.ts
src/app/api/admin/blogs/route.ts
src/lib/load-blog.ts
src/components/markdown-image.tsx
src/app/write/components/sections/images-section.tsx
src/app/write/components/sections/cover-section.tsx
```

## 14.4 首页布局 / 卡片位置 / 顶栏问题

```txt
src/components/nav-card.tsx
src/app/(home)/home-draggable-layer.tsx
src/app/(home)/stores/config-store.ts
src/app/(home)/stores/layout-edit-store.ts
src/hooks/use-center.ts
src/hooks/use-size.ts
```

## 14.5 favicon / avatar 问题

```txt
src/lib/local-admin/site-assets.ts
src/hooks/use-site-assets.ts
src/app/api/site-assets/avatar/route.ts
src/app/api/site-assets/favicon/route.ts
src/app/api/site-assets/version/route.ts
src/layout/head.tsx
```

## 14.6 资源清理误判

```txt
src/lib/local-admin/asset-inventory.ts
src/app/api/admin/assets/route.ts
```

---

# 15. 最近修改记录（给后续接手者最有用的部分）

## 15.1 favicon/avatar 实时更新修复

关键提交：

```txt
8efb246 Rewrite mutable site asset handling
```

相关历史提交：

```txt
5021d9f Fix favicon and avatar runtime uploads
cb030a8 Serve favicon from runtime data
12a7acf Harden public image upload persistence
cc6d70a Fix public image uploads from create dialogs
ed08828 Fix local public image URL normalization
```

用户已经确认这条链路可用。

## 15.2 资源清理器可视化管理

相关提交：

```txt
5da635c Add visual asset cleanup manager
e752cb6 Fix asset inventory protected display
9280fcd Sync settings after asset cleanup
```

## 15.3 应用导航页 `/apps`

这是后加功能，不是原项目自带。

相关核心文件已经在前面列过。


## 15.6 2026-05-18 接手后的完整问题总结

这一段是从本轮接手到现在的集中复盘，后续智能体一定要先读。这里记录的不只是“改了什么”，更重要的是：**哪些判断错过、哪些坑反复出现、最后怎么收敛**。

### 15.6.1 真实项目路径一开始容易搞错

最开始不要在 OpenClaw agent workspace 里找源码。真实项目在：

```txt
/root/2025-blog-local-docker
```

agent workspace 只是助手自己的工作区，里面可能只有记忆、上下文、临时文件，不是博客源码。

后续接手时先执行：

```bash
cd /root/2025-blog-local-docker
git status --short
```

### 15.6.2 导航白色滑块问题：不要再回到共享跨形态动画思路

用户最早反馈的是导航白色滑块错位、首页默认白块不符合预期、从首页切到顶部 icons 导航时表现异常。

曾经尝试/分析过这些方向：

- 让 full 导航和 icons 导航共享滑块动画。
- 只在同一导航形态内保留动画，跨形态首次进入直接落位。
- 延迟显示白块，等待布局稳定。
- 怀疑 `Card` 外层 `motion.div` 的 `scale: 0.6 -> 1` 进场动画污染测量。
- 给线上加 `navDebug=1` 调试信息，用来确认 `pathname / activeIndex / highlight / item rects`。

经验结论：

- full 侧边导航和 top icons 导航不是一个稳定的共享布局动画场景。
- 继续在跨形态共享动画上硬修，很容易陷入“本地看似正常，线上首次进入错位”。
- 现在代码已改为更稳定的 fixed 尺寸和索引定位方式，不再依赖复杂 DOM 测量。
- 首页 `/` 不应该默认 active 到第一项，所以 `activeIndex` 找不到匹配时应返回 `null`。
- 首页默认不显示白块，但 hover 导航项仍要显示白块。

关键文件：

```txt
src/components/nav-card.tsx
src/components/card.tsx
src/app/(home)/home-draggable-layer.tsx
```

### 15.6.3 导航图标替换：zip 目录名和实际文件名要做映射

用户多次给过图标 zip/XML，希望统一导航图标风格。

踩坑点：

- zip 里可能都是 `apps-filled.svg` / `apps-outline.svg` 这种通用文件名。
- 项目里实际文件名按功能分开：`scroll-*`、`projects-*`、`website-*` 等。
- 不能机械地按 zip 文件名覆盖，否则图标会错位。

当前导航顺序：

```txt
我的文章 -> 应用导航 -> 我的项目 -> 优秀博客 -> 推荐分享 -> 关于网站
```

对应 SVG：

```txt
我的文章: src/svgs/scroll-outline.svg / src/svgs/scroll-filled.svg
应用导航: src/svgs/apps-outline.svg / src/svgs/apps-filled.svg
我的项目: src/svgs/projects-outline.svg / src/svgs/projects-filled.svg
优秀博客: src/svgs/website-outline.svg / src/svgs/website-filled.svg
推荐分享: src/svgs/share-outline.svg / src/svgs/share-filled.svg
关于网站: src/svgs/about-outline.svg / src/svgs/about-filled.svg
```

### 15.6.4 内容页旧数据闪一下：不是接口错，是初始 state 错

用户发现某些页面会先显示旧内容，再切换成最新内容。

根因：

- 页面把源码里的 `list.json` 当成初始 state 直接渲染。
- 客户端 API 请求回来后再覆盖，所以用户看到“旧数据闪一下”。

修法：

- 不再把 `list.json` 作为首屏 state 直接渲染。
- `list.json` 只能作为 API 失败或离线时的 fallback。

已处理页面包括：

```txt
src/app/projects/page.tsx
src/app/share/page.tsx
src/app/bloggers/page.tsx
src/app/apps/page.tsx
src/app/pictures/page.tsx
src/app/snippets/page.tsx
```

后续新增内容页时，不要再复用“list.json 直接首屏渲染”的旧写法。

### 15.6.5 写作页图片插入：必须同时照顾桌面拖拽和移动端点击

用户明确要：

- 桌面端继续支持拖拽图片到编辑器。
- 移动端要能点按钮把图片插入文章，因为手机没有拖拽。

当前实现核心：

```txt
src/app/write/stores/write-store.ts
src/app/write/components/editor.tsx
src/app/write/components/sections/images-section.tsx
```

要点：

- `write-store` 保存 textarea 引用：`editorElement`。
- `insertAtCursor(text)` 优先在光标处插入。
- 如果没有 textarea 引用，就追加到 markdown 末尾。
- 图片卡片下面有 `填 入` 按钮，点击后插入 markdown 图片语法。
- 拖拽能力仍然保留，不要删。

### 15.6.6 图片卡片样式：用户偏好已经反复确认

当前偏好：

- `填 入` 两字中间有空格。
- `填 入` 是红色、加粗，字号比最初 `6px` 稍大。
- `填 入` 按钮在缩略图下方，不在图内。
- 封面标识是整张图上的白色磨砂遮罩。
- “封面”两个字红色、加粗。
- 遮罩透明度用 inline style 控制，避免 Tailwind arbitrary opacity 在生产里不明显或被 blur 视觉掩盖。

相关代码位置：

```txt
src/app/write/components/sections/images-section.tsx
```

如果用户说透明度“不明显”，不要只改 `bg-white/[...]`，优先检查 inline `background` 和 `backdropFilter`。

### 15.6.7 单篇文章编辑页右上角按钮：曾经误判成 `/blog` 批量编辑

这是本轮最容易误解的一点。

用户说：

> 文章编辑页面上面的取消和删除按钮

真正指的是：

```txt
/blog/[id] 文章详情页 -> 点击右上角“编辑” -> 进入 /write/[slug]
```

也就是 `/write/[slug]` 的右上角功能栏。

不是 `/blog` 文章列表页的批量编辑模式。

相关文件：

```txt
src/app/write/components/actions.tsx
```

最终稳定方案：

- 功能栏使用 `fixed top-4 right-6 z-[9999]`。
- 删除/取消按钮改成原生 `<button>`，不是 `motion.button`。
- 取消按钮直接返回原文章页，不再用浏览器 `confirm()`。
- 删除按钮不再用浏览器 `confirm()`，改成页面内确认条。
- 删除确认条绝对定位在删除按钮正下方：`absolute top-full right-0 mt-2`。
- 确认条不参与功能栏 flex 布局，不会把功能栏撑开。

用户已经确认“可以了”。

### 15.6.8 `/blog` 批量编辑页也改过，但不是用户最后要的那个问题

中途曾经把 `/blog` 文章列表页批量编辑模式右上角工具栏也提高了层级：

```txt
src/app/blog/page.tsx
```

这不是最终用户说的“某一篇文章右上角编辑模式”，但改动本身可以保留。以后如果出现 `/blog` 列表页批量编辑按钮点不到，可先看这里。

### 15.6.9 Docker 构建：npm 在容器内出过问题，当前使用 pnpm

遇到过两个 Docker 构建坑：

1. BuildKit 拉 `node:20-bookworm-slim` 时遇到 content descriptor / in-toto 相关错误。
2. 容器里 `npm install` 出过：`npm error Exit handler never called!`

最终处理：

- 手动拉过 base image。
- Dockerfile 改为 pnpm 安装依赖，匹配 `packageManager: pnpm@10.15.0`。

当前 Dockerfile 要点：

```txt
corepack enable
corepack prepare pnpm@10.15.0 --activate
pnpm install --frozen-lockfile
```

不要轻易改回 npm。

### 15.6.10 本地 UI 没变化时，优先怀疑容器没重建

多次出现：代码已提交/推送，但用户本地页面“没变化”。

根因通常不是代码没改，而是 Docker 容器还在跑旧镜像。

UI 改动要让本地站点立刻可见，需要：

```bash
DOCKER_BUILDKIT=0 docker compose up -d --build
```

当前容器信息：

```txt
container: 2025-blog-local-docker
service: blog
port: 3000:3000
```

### 15.6.11 推送远程：当前使用 SSH，两个远程都要推

当前远程：

```txt
github -> git@github.com:duanxiaosheng/2025-blog-public-docker.git
origin -> git@gitee.com:duanxiaosheng/2025-blog-local-docker.git
```

本轮推送一直使用：

```bash
git push github main
git push origin main
```

文档里旧的 `git push gitee main` 可能不是当前 remote 名称，接手时先 `git remote -v` 看实际名字。

### 15.6.12 最近关键提交索引

这一轮比较关键的提交包括：

```txt
54ecd7d Restore full nav highlight size
1459646 Tighten nav spacing and avoid stale list flash
316a095 Avoid stale list flash on remaining content pages
f73fefd Hide homepage nav highlight on home route
39d704e Replace apps nav icons
6ff3aa1 Reorder nav items and replace nav icons
f034e8c Update projects nav icon
058cc55 Add image insert action in writer
1d8a6e0 Move writer image insert button below thumbnail
93296e9 Use pnpm for Docker install
b9309f0 Simplify writer image insert controls
4f49530 Tune writer image insert labels
f9f1cdb Add frosted cover badge background
d5c32c0 Make cover thumbnail overlay frosted
400a0ca Lower cover overlay opacity
5b78836 Adjust cover overlay opacity to thirty percent
38ff134 Use explicit cover overlay opacity
305bb57 Fix write page action controls
a951c8f Raise writer action bar above overlays
2f742a5 Tune writer image labels
cbcc07b Fix article edit toolbar controls
221ea3e Adjust single article edit actions
a0b9b2b Make article edit actions native buttons
11734eb Float delete confirmation above button
a63911e Move delete confirmation below button
d6a5113 Refresh project documentation
```

### 15.6.13 给下一个智能体的建议

如果继续接这个项目，优先按这个顺序处理问题：

1. 先确认用户说的是哪个页面：`/blog`、`/blog/[id]`、`/write`、还是 `/write/[slug]`。
2. 再确认本地 Docker 是否已经重建，别只看 Git 提交。
3. UI 视觉问题先找相关组件，不要先动数据层。
4. 图片问题先查路径和 `blob:`，不要先怀疑上传控件。
5. 导航问题先看 `src/components/nav-card.tsx`，并记住首页 `/` 不默认 active。
6. 写作页按钮问题先看 `src/app/write/components/actions.tsx`。
7. 每次改完：构建、提交、推 GitHub/Gitee、必要时重建 Docker。
8. 文档也要同步更新，否则下一个智能体会重新踩坑。

---

## 15.5 Docker Hub 一键部署与发布链路

已新增：

```txt
.github/workflows/docker-image.yml
docker-compose.release.yml
.env.example
README.md
BAOTA-DEPLOY.md
```

当前发布方向：

- 开发/本地改代码：继续使用 `docker-compose.yml`（`build: .`）
- 线上/宝塔快速部署：使用 Docker Hub 镜像 `duanxiaosheng/2025-blog-local-docker:latest`
- GitHub Actions 会在推送 `main` 后自动构建并推送 Docker Hub / GHCR 镜像

注意：

- Docker Hub 自动推送依赖 GitHub Secrets：`DOCKERHUB_USERNAME`、`DOCKERHUB_TOKEN`
- 宝塔一键部署优先推荐 `docker run` 或 `docker-compose.release.yml`，不要让普通用户在服务器现 build
- 运行时数据仍然只认宿主机挂载的 `data/`，不要打进镜像

## 15.4 导航栏与优秀博客点击问题、hover 对齐问题

最近相关提交：

```txt
e430ab0 Fix nav click hit area and blogger link targets
e64bb04 Fix nav hover highlight alignment
9ea52c7 Tweak nav hover highlight size
a748cd6 Tighten nav icon spacing
```

---

# 16. 验证方法与命令

## 16.1 最少验证

每次代码修改后至少跑：

```bash
pnpm build
```

## 16.2 数据 / 图片链路验证

如果改了图片、内容存储、资源路径、公共图片上传、文章图片上传，必须再跑：

```bash
pnpm check:data
```

## 16.3 线上接口快速验证

```bash
curl -s http://81.70.46.21:3000/api/content/share
curl -s http://81.70.46.21:3000/api/content/projects
curl -s http://81.70.46.21:3000/api/content/bloggers
curl -s http://81.70.46.21:3000/api/content/apps
curl -I http://81.70.46.21:3000/api/site-assets/avatar
curl -I http://81.70.46.21:3000/api/site-assets/favicon
curl -s http://81.70.46.21:3000/api/site-assets/version
```

## 16.4 有图片问题时的专项排查

```bash
curl -s http://<host>:3000/api/content/share | grep blob || true
curl -s http://<host>:3000/api/content/projects | grep blob || true
curl -s http://<host>:3000/api/content/bloggers | grep blob || true
curl -s http://<host>:3000/api/content/apps | grep blob || true
curl -s http://<host>:3000/api/content/pictures | grep blob || true
```

---

# 17. Git / 提交 / 推送工作流

## 17.1 标准流程

```bash
pnpm build
# 如涉及数据/图片链路，再跑 pnpm check:data

# 每次准备 commit/push 前，先更新本交接文档

git status --short
git add <changed-files> 给下一个智能体也可以编辑此仓库.md
git commit -m "..."
git push origin main
git push gitee main
```

## 17.2 Gitee 如果提示 fetch first

不要硬推，先：

```bash
git fetch gitee main
git status
git log --oneline --graph --decorate --all -20
```

然后再决定 merge / rebase。

## 17.3 强规则

**以后每次推 Git 前，都要顺手更新这份文档。**

哪怕只是：

- 某个 hover 微调
- 某个资源路径坑
- 某个上传弹窗问题

也至少在“最近修改记录”里补一句。

否则后续接手者会重新猜一遍，浪费时间。

---

# 18. 与用户沟通方式

用户叫小东。

偏好：

- 像朋友，不要客服腔
- 先给判断，再给解释
- 技术建议要直接、务实
- 不想重复讲背景
- 更偏好长期可维护方案

如果接手者遇到这个项目问题，默认动作应该是：

1. 进入项目目录
2. 看代码 / 看接口 / 看线上返回
3. 直接修
4. `pnpm build`
5. 更新本交接文档
6. commit + push GitHub/Gitee
7. 用简短的话告诉用户根因和结果

不要：

- 让用户再解释项目目标
- 没验证就说“好了”
- 把旧 GitHub App 模型当成正确方向

---

# 19. 这份文档存在的意义

如果你是下一个接手本仓库的智能体，请你默认相信下面这三条：

1. 这个项目的真实目标不是“保留原项目玩法”，而是“做成可长期用的自托管博客后台”。
2. 这个项目最容易出问题的不是普通页面，而是：**图片路径、运行时数据落盘、缓存、导航交互、可视化编辑链路**。
3. 改完代码后，**验证 + 更新这份文档**，和修代码本身一样重要。

如果你把这三条记住，后面大多数坑都能绕开。
